{
  "name": "automat",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "plan": "bun run index.ts",
    "test": "prettier --write --log-level=warn . && tsc && bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.9.5"
  },
  "peerDependencies": {
    "typescript": "^6"
  },
  "dependencies": {
    "@doist/todoist-sdk": "^10.5.1",
    "smol-toml": "^1.7.0",
    "zod": "^4.4.3"
  }
}
