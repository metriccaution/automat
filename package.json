{
  "name": "automat",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "plan": "bun run index.ts",
    "test": "prettier --write --log-level=warn . && tsc && bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.9.6"
  },
  "peerDependencies": {
    "typescript": "^6"
  },
  "dependencies": {
    "@doist/todoist-sdk": "^13.0.1",
    "smol-toml": "^1.7.1",
    "zod": "^4.4.3"
  }
}
