{
  "name": "automat-airtable",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node dist",
    "test": "npm run lint && npm run format && npm run compile && npm run unit-test",
    "format": "npm run format:docs && npm run format:code && npm run format:config",
    "format:code": "prettier --write 'src/**/*'",
    "format:config": "prettier --write '*.json'",
    "format:docs": "prettier --write '*.md'",
    "compile": "tsc",
    "lint": "tslint -p . --fix",
    "unit-test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@types/convict": "^4.2.1",
    "@types/lodash": "^4.14.119",
    "@types/node": "^10.12.18",
    "@types/p-retry": "^2.0.0",
    "airtable": "^0.5.8",
    "axios": "^0.18.0",
    "convict": "^4.4.1",
    "lodash": "^4.17.11",
    "p-map": "^2.0.0",
    "p-retry": "^3.0.0"
  },
  "devDependencies": {
    "@types/jest": "^23.3.11",
    "husky": "^1.3.1",
    "jest": "^23.6.0",
    "ts-jest": "^23.10.5",
    "tslint": "^5.12.0",
    "tslint-config-prettier": "^1.17.0",
    "typescript": "^3.2.2"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm test"
    }
  }
}
